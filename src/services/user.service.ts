/* import { Car } from '../interfaces/car.interface';
import ItemModel from '../models/model.item'; */
export class UserServices {
/*     static getCars = async () => {
        const response = await ItemModel.find({});
        return response;
    };

    static getCar = async (id: string) => {
        const response = await ItemModel.findById(id);
        return response;
    };

    static deleteCar = async (id: string) => {
        const response = await ItemModel.findByIdAndDelete(id);
        return response;
    };

    static insertCar = async (item: Car) => {
        const response = await ItemModel.create(item);
        return response;
    };

    static updateCar = async (id: string, data: Car) => {
        const response = await ItemModel.findByIdAndUpdate(id, data, {
            new: true,
        });
        return response;
    }; */
}
